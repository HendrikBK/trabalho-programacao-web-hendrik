export class Animal {
    id: number;
    nome: string;
    idade: number;
    especie: String;
    constructor(id: number, nome: string, idade: number, especie: string) {
        this.id = id,
        this.nome = nome,
        this.idade = idade,
        this.especie = especie
    }
}
